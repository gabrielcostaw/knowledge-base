[build]
  command = "cd frontend && npm install && npm run build & BUILD_PID=$! && wait $BUILD_PID; pkill -f vue-cli-service || true; exit 0"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "16"
  CI = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200